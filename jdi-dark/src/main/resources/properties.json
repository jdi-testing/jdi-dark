{
  "properties": [
    {
      "name": "domain",
      "description": "Specify the domain(domain=URI) or domains(domain=nameDomain=URI, nameDomain2=URI). These variables can be used in @ServiceDomain annotation"
    },
    {
      "name": "log.level",
      "description": "Setting the project logging level",
      "values": [
        "INFO ",
        "DEBUG",
        "STEP",
        "ERROR",
        "OFF"
      ]
    },
    {
      "name": "allure.logger",
      "description": "Wright to allure logger. Default = true"
    }
  ]
}